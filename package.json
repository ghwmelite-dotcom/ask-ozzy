{
  "name": "ghana-civil-ai",
  "version": "1.0.0",
  "description": "AI-powered assistant platform for GoG operations",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute ghana-civil-ai-db --local --file=schema.sql",
    "db:init:remote": "wrangler d1 execute ghana-civil-ai-db --remote --file=schema.sql"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "sharp": "^0.34.5",
    "typescript": "^5.7.0",
    "wrangler": "^3.99.0"
  }
}
